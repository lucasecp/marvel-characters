import styled, { css } from "styled-components";

interface HeroInfoProps {
  isFill: Boolean | undefined;
}

export const Container = styled.div`
  > header {
 
  }
`;
export const HeroInfo = styled.div<HeroInfoProps>``;
